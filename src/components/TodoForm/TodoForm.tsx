import React from 'react';

const TodoForm = () => {
  return (
    <input
      type="text"
      className="new-todo"
      placeholder="What needs to be done?"
    />
  );
};

export default TodoForm;
